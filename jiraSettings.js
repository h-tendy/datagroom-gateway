let host = 'jira.yourcompany.com'
let settings = {
    protocol: 'https',
    host,
    username: 'user',
    password: 'password',
    apiVersion: '2',
    strictSSL: false
}

module.exports = {
    host,
    settings
}
